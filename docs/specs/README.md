# Specifications

`/to-spec` publishes one approved specification per Markdown file in this directory.

Use `docs/specs/<feature-slug>.md` and include a `Status:` field from `docs/agents/triage-labels.md`. A published specification is normally `Status: ready`.

No technical specification has been generated by the workflow setup itself.
