# Use a time-constrained browser prototype

Status: accepted by project owner on 2026-09-10

Both E4 attempts remain failed and their evidence is unchanged. For the prototype deadline, use the already cached `@qvac/sdk` 0.19.0 `QWEN3_1_7B_INST_Q4` model on the RTX 4050 through a loopback-only Node.js host, with a responsive browser interface and local file persistence on the same Windows laptop; this is a time-constrained exception that does not claim E4 passed or authorize production readiness. Cloud and delegated inference remain excluded, phone access and packaging are deferred, and Ticket 02 remains blocked while this provisional topology is demonstrated only from the laptop.
