# Local tickets

`/to-tickets` publishes one ticket per Markdown file under `docs/tickets/<feature-slug>/`.

Number tickets from `01` in dependency order. Each ticket declares its blocking paths, observable acceptance criteria, and one status from `docs/agents/triage-labels.md`.

The workflow setup does not create implementation tickets.
